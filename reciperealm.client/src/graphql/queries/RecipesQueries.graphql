query UserRecipes($email: String!) {
  userRecipes(email: $email) {
    userRecipes {
      name
      ingredients
      likes
      commentsCount
    }
    userRecipes {
      name
      cookingTime
      difficulty
      ingredients
      cookingSteps
      likes
      commentsCount
      userId
    }
    error
  }
}
